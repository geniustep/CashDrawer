# متطلبات التشغيل فقط (بدون PyInstaller)
# استخدم: pip install -r requirements-run.txt
# ملاحظة: websockets>=11 مطلوب لعميل WebSocket (معامل additional_headers)

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=11.0
pydantic>=2.5.0
pywin32>=306
